<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01"
        aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <a class="navbar-brand" href="#">Treble Finder</a>
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active">
                <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Find Talent</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Edit Profile</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Leave A Review</a>
            </li>
        </ul>
        <ul class="nav justify-content-end">
            <li><a href="#"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
            <li><a href="/login"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
        </ul>
    </div>
</nav>


<div class="container">
    <div class="row">
        <div class="col-md-6">
            {{user.image}}
        </div>
        <div class="col-md-6">
            {{user.bio}}
        </div>

    </div>
    <div class="row">

        <div class="col-md-6">
            <span>{{user.firstName}}'s Email: {{user.email}}</span> <span>{{user.firstName}}'s
                Phone:{{user.phone}}</span>
        </div>

        <div class="col-md-6">
            Talents
            <ul>
                {{#each user.Skills}}
                <li>{{name}}</li>
                {{/each}}
            </ul>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            Genres
            <ul>
                {{#each user.Genres}}
                <li>{{name}}</li>
                {{/each}}
            </ul>
        </div>
        <div class="col-md-6">
            Areas
            <ul>
                {{#each user.Areas}}
                <li>{{name}}</li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>

<button type="button" class="btn btn-secondary"><a href="mailto:{{user.email}}?Subject=Hello%20again" target="_top">Send
        Email</a></button>